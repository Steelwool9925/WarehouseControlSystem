# plan-feature pipeline artifacts (see .claude/skills/_shared/pipeline-contract.md)
.claude/maps/
.claude/plans/
.claude/reports/

# .NET build output (added early — the first backend plan already produces these)
backend/**/bin/
backend/**/obj/
backend/**/TestResults/

# append-only dev changelog (cleanup-crew) — local record, not part of the commit
changes.md

# frontend build output/deps (added early — the first frontend plan already produces these)
frontend/node_modules/
frontend/dist/
